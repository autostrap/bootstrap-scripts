#!/bin/sh

# 2015, j.grassler@syseleven.de

# Launches a project's additional bootstrap script if present.

if [ -x ${config_dir}/scripts.d/additional ]; then
  export bootstrap_stage=additional_script
  launch_logged ${config_dir}/scripts.d/additional 
  log_stage_finished
fi
